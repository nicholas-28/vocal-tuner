import { PITCH_CALIBRATION } from '../calibration/pitchCalibration';

// Instantaneous band occupancy, not a vibrato-aware exercise success rule.
export const PRACTICE_BAND_CENTS = PITCH_CALIBRATION.inTuneCents;
export const MAX_ACCOUNTABLE_PRACTICE_INTERVAL_MS = 250;
export const PRACTICE_DISPLAY_TICK_MS = 250;
